node_modules/
dist/
.DS_Store
.env.local
.env*.local
.worktrees/

# Supabase CLI local link metadata (project ref, pooler URL/host, versions).
# Not secrets, but unnecessary recon — keep it out of the public repo (MED-6).
supabase/.temp/
